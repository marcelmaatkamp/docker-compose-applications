<source>
  @type  forward
  @id    input1
  @label @input
  port  24224
</source>

<source>
  type syslog
  tag graylog2
  @label @input
</source>

<label @input>
 <match **>
  type amqp
  exchange amq.direct
  host rabbitmq
  port 5672
  vhost /
  user guest
  pass guest
  format json
  tag_key true
 </match>
</label>

<source>
  type amqp
  host rabbitmq
  port 5672
  vhost /
  user guest
  pass guest
  queue my_queue
  format json
  tag_key true
  @label @amqp
</source>

<label @amqp>
 <match **>
  type copy
  <store>
    type gelf
    host graylog
    port 12201
    flush_interval 5s
  </store>
  <store>
    type stdout
  </store>
 </match>
</label>
